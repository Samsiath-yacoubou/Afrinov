<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inscription | Votre Marque</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #7B61FF;
            --primary-dark: #5D47E5;
            --secondary: #FF7D61;
            --dark: #1A1A2E;
            --light: #F8F9FF;
            --gray: #E2E4F0;
        }
        
        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--light);
            color: var(--dark);
            line-height: 1.7;
            overflow-x: hidden;
        }
        
        .auth-wrapper {
            display: flex;
            min-height: 100vh;
            position: relative;
        }
        
        .auth-illustration {
            flex: 1;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            display: none;
            align-items: center;
            justify-content: center;
            padding: 4rem;
            position: relative;
            overflow: hidden;
        }
        
        .auth-illustration::before {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            border-radius: 50%;
            background: rgba(255,255,255,0.1);
            top: -100px;
            right: -100px;
        }
        
        .auth-illustration::after {
            content: '';
            position: absolute;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: rgba(255,255,255,0.08);
            bottom: -50px;
            left: -50px;
        }
        
        .illustration-content {
            position: relative;
            z-index: 2;
            max-width: 500px;
            color: white;
        }
        
        .illustration-content h2 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            line-height: 1.3;
        }
        
        .illustration-content p {
            opacity: 0.9;
            font-size: 1.1rem;
        }
        
        .auth-form {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            background-color: white;
        }
        
        .form-container {
            width: 100%;
            max-width: 420px;
            padding: 2.5rem;
        }
        
        .logo {
            text-align: center;
            margin-bottom: 2.5rem;
        }
        
        .logo svg {
            width: 48px;
            height: 48px;
            margin-bottom: 1rem;
        }
        
        .logo h1 {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--primary);
            margin: 0;
        }
        
        .form-title {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--dark);
        }
        
        .form-control {
            height: 52px;
            border-radius: 10px;
            border: 2px solid var(--gray);
            padding: 0 1.25rem;
            margin-bottom: 1.25rem;
            font-size: 1rem;
            transition: all 0.3s ease;
        }
        
        .form-control:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(123, 97, 255, 0.15);
        }
        
        .btn-primary {
            background-color: var(--primary);
            border: none;
            height: 52px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 1rem;
            width: 100%;
            margin-top: 0.5rem;
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
        }
        
        .form-check-input {
            width: 20px;
            height: 20px;
            margin-top: 0.3rem;
            border: 2px solid var(--gray);
        }
        
        .form-check-input:checked {
            background-color: var(--primary);
            border-color: var(--primary);
        }
        
        .form-check-label {
            margin-left: 0.5rem;
            font-size: 0.9375rem;
        }
        
        .auth-footer {
            text-align: center;
            margin-top: 2.5rem;
            font-size: 0.9375rem;
            color: #6B7280;
        }
        
        .auth-footer a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s ease;
        }
        
        .auth-footer a:hover {
            color: var(--primary-dark);
            text-decoration: underline;
        }
        
        .divider {
            display: flex;
            align-items: center;
            margin: 1.5rem 0;
            color: #6B7280;
            font-size: 0.875rem;
        }
        
        .divider::before, .divider::after {
            content: '';
            flex: 1;
            border-bottom: 1px solid var(--gray);
        }
        
        .divider::before {
            margin-right: 1rem;
        }
        
        .divider::after {
            margin-left: 1rem;
        }
        
        @media (min-width: 992px) {
            .auth-illustration {
                display: flex;
            }
            .auth-form {
                padding: 4rem;
            }
        }
        
        /* Animation */
        [data-aos="fade-up"] {
            transform: translateY(30px);
            opacity: 0;
            transition-property: transform, opacity;
        }
        
        [data-aos="fade-up"].aos-animate {
            transform: translateY(0);
            opacity: 1;
        }
    </style>
</head>
<body>
    <div class="auth-wrapper">
        <div class="auth-illustration" data-aos="fade">
            <div class="illustration-content">
                <h2>Rejoignez notre communauté créative</h2>
                <p>Connectez-vous avec des professionnels inspirants et donnez vie à vos idées les plus audacieuses.</p>
                <div style="margin-top: 3rem;">
                    {# <img src="https://illustrations.popsy.co/amber/sign-up.svg" alt="Sign up illustration" style="max-width: 100%;"> #}
                </div>
            </div>
        </div>
        
        <div class="auth-form">
            <div class="form-container">
                <div class="logo" data-aos="fade-up">
                    {# <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <rect width="48" height="48" rx="12" fill="#7B61FF"/>
                        <path d="M24 36C30.6274 36 36 30.6274 36 24C36 17.3726 30.6274 12 24 12C17.3726 12 12 17.3726 12 24C12 30.6274 17.3726 36 24 36Z" fill="white"/>
                        <path d="M24 28C26.2091 28 28 26.2091 28 24C28 21.7909 26.2091 20 24 20C21.7909 20 20 21.7909 20 24C20 26.2091 21.7909 28 24 28Z" fill="#7B61FF"/>
                    </svg>
                    <h1>VotreMarque</h1> #}
                </div>
                
                <h1 class="form-title" data-aos="fade-up" data-aos-delay="100">Créer un compte</h1>
                
                {% for flash_error in app.flashes('verify_email_error') %}
                    <div class="alert alert-danger" role="alert" data-aos="fade-up" data-aos-delay="150">{{ flash_error }}</div>
                {% endfor %}
                
                {{ form_errors(registrationForm) }}
                
                {{ form_start(registrationForm, {'attr': {'data-aos': 'fade-up', 'data-aos-delay': '200'}}) }}
                    <div class="row">
                        <div class="col-md-6">
                            {{ form_row(registrationForm.nom, {
                                'attr': {
                                    'class': 'form-control',
                                    'placeholder': 'Nom'
                                },
                                'label': false
                            }) }}
                        </div>
                        <div class="col-md-6">
                            {{ form_row(registrationForm.prenom, {
                                'attr': {
                                    'class': 'form-control',
                                    'placeholder': 'Prénom'
                                },
                                'label': false
                            }) }}
                        </div>
                    </div>
                    
                    {{ form_row(registrationForm.email, {
                        'attr': {
                            'class': 'form-control',
                            'placeholder': 'Adresse email'
                        },
                        'label': false
                    }) }}
                    
                    {{ form_row(registrationForm.plainPassword, {
                        'attr': {
                            'class': 'form-control',
                            'placeholder': 'Mot de passe'
                        },
                        'label': false
                    }) }}
                    
                    <div class="form-check mb-4" data-aos="fade-up" data-aos-delay="250">
                        {{ form_widget(registrationForm.agreeTerms, {
                            'attr': {'class': 'form-check-input'}
                        }) }}
                        {{ form_label(registrationForm.agreeTerms, 'J\'accepte les conditions d\'utilisation', {
                            'label_attr': {'class': 'form-check-label'}
                        }) }}
                    </div>
                    
                    <button type="submit" class="btn btn-primary" data-aos="fade-up" data-aos-delay="300">
                        S'inscrire
                    </button>
                {{ form_end(registrationForm) }}
                
                <div class="divider" data-aos="fade-up" data-aos-delay="350">ou</div>
                
                <div data-aos="fade-up" data-aos-delay="400">
                    <button class="btn btn-outline-secondary" style="height: 52px; width: 100%; border-radius: 10px; border: 2px solid var(--gray); font-weight: 600;">
                        Continuer avec Google
                    </button>
                </div>
                
                <div class="auth-footer" data-aos="fade-up" data-aos-delay="450">
                    Déjà un compte? <a href="{{ path('app_login') }}">Se connecter</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script>
        AOS.init({
            duration: 800,
            once: true,
            easing: 'ease-out-quart'
        });
    </script>
</body>
</html>